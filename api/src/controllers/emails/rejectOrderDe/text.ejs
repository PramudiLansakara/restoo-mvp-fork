    BESTELLUNG LEIDER ABGELEHNT!
    BESTELLUNGSNUMMER #<%= reference %>
    <%= cancelledAt %>

    Sehr geehrte/r <%= customer.name %>, wir bedauern, Ihnen mitteilen zu müssen, dass Ihre Bestellanfrage abgelehnt wurde.

<% items.forEach(function(item){ %>
    <%= item.name %> 
    Größe: <%= item.priceDetails.name %>  
    x <%= item.priceDetails.quantity %>  <%= item.priceDetails.price %>€
<%  }); %> 

<% if(discount){ %>
    Rabatt: <%= discount %>%
<%  }; %>
<% if(deliveryCharge){ %>
    Fahrtkosten: <%= deliveryCharge %>€
<%  }; %>
    
    Gesamtkosten (inkl. Mwst): <%= total %>€

    Kunde: <%= customer.name %>
    Telefonnummer: <%= customer.phoneNumber %>
    Email: <%= customer.email %>
    Kommentar: <%= customer.note %>
    Bestellungsart: <%= orderType %>
    Bestellungs-Status: <%= status %>
    Datum: <%= placedAt %>
    Bestellungsnummer: #<%= reference %>

    Wir stehen Ihnen für zukünftige Bestellungen gerne zur Verfügung.